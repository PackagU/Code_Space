# 08. 안전/보안 게이트 — 모든 단계의 종료 조건

> 상태: 상시 적용 · 담당: 전원 · 각 단계 완료 선언 전에 해당 절 체크 · 갱신: 2026-07-03
> 메모: 소프트 E-stop 토픽/물리 E-stop(Han) 미구현 — 회의 안건. secrets 게이트는
> .gitignore(.env)+push 전 스캔으로 운영, 이식성 게이트는 scripts/check_portability.py.

## 안전 (사람과 로봇)

### 정지 수단 (R1 실기 전 필수)

- [ ] 물리 E-stop: 모터 전원을 직접 끊는 버튼 (소프트웨어 경유 금지) — Han 협의
- [ ] 소프트 E-stop: `/emergency_stop` 토픽 → cmd_vel 게이트 노드가 0속도 발행 + Nav2 goal 취소. 어느 단계에서든 1초 내 정지
- [ ] 통신 두절 시 정지: cmd_vel 게이트에 watchdog (N초간 상위 명령 없으면 0속도)

### 속도/거리 정책

- [ ] 최대 속도 상한을 한 곳(yaml)에 정의하고 URDF/Nav2/게이트 노드가 공유 — 사람 있는 환경 기준 0.5m/s 이하 권장
- [ ] collision monitor: 정지 존(전방 즉시 정지)과 감속 존 치수 문서화
- [ ] 2D LiDAR 한계(발/낮은 장애물 미탐지)를 시연 계획에 명시 — 통제 구역 운영으로 보상

### 고장 시 기본값 = 정지

- [ ] 모든 실패 경로(맵 로드 실패, 플래너 실패, 엘베 무응답, 노드 다운)가 "정지"로 수렴하는지 단계별 고장 주입으로 확인 (06 문서)
- [ ] 리프트 승강 중/로봇팔 전개 중 주행 금지 인터락 — Han/Kim 협의
- [ ] 배터리 저전압 시 동작 정의 (경고 → 안전 정지)

## 보안 (데이터와 시스템)

### Secrets (AGENTS.md 7~9 재확인, 상시)

- [ ] `.env`, API 토큰, SSH 키는 repo/Docker 이미지/Notion/로그에 절대 미포함. `.dockerignore`와 `.gitignore` 점검
- [ ] Docker 이미지 publish 전 `docker history`로 레이어에 secret 없는지 확인
- [ ] GHCR 이미지/repo는 private 유지, Jetson에는 read-only pull 토큰만 배치

### Jetson 시스템 (R1 전)

- [ ] 기본 계정 비밀번호 변경, SSH 키 인증 + 비밀번호 로그인 비활성
- [ ] 방화벽(ufw): SSH 외 인바운드 차단, DDS 포트는 팀 장비 대역만
- [ ] 불필요 서비스 비활성, sudo 권한 최소화

### ROS2 네트워크

- [ ] `ROS_DOMAIN_ID` 팀 고정값 사용 — 같은 공유기의 타 ROS 장비와 격리
- [ ] 공용 Wi-Fi 금지 — 시연/테스트는 전용 AP 또는 핫스팟. 외부망에서 cmd_vel 주입 가능성 차단이 목적
- [ ] 원격 모니터링은 같은 AP 내 데스크탑 RViz로 한정. (SROS2 암호화는 시간 여유 시 검토 항목 — 필수 아님)

### 운영/물리

- [ ] 시연 중 로봇 반경 접근 통제 담당자 1인 지정
- [ ] 미션 로그/rosbag에 개인 식별 정보(얼굴 영상 등) 포함 시 외부 업로드 금지 — 웹캠 사용 시 특히 주의
- [ ] 사고/이상 동작 발생 시 기록 양식: 시간, 단계, 로그 경로, 조치 — `docs/improvement_report.md`에 등재

## 단계별 게이트 요약

| 단계 | 통과해야 할 절 |
|------|---------------|
| 01~05 (시뮬) | "고장 시 기본값 = 정지" 중 해당 항목 + Secrets 상시 항목 |
| 06 | 안전 절 전체 (소프트 E-stop 포함) |
| 07 R1 | 이 문서 전체 — 물리 E-stop 없이는 실기 금지 |
