# Journal 2026-06-02

작성일: 2026-06-02

## 요약

2026-06-02에는 엘리베이터 층 이동 확인 이후 다음 단계인 자동 맵 전환을 바로 구현하지 않고, 새 테스트 폴더에서 계획과 분석을 먼저 정리했다.

## 사용자 결정

- 오늘은 자동 맵 전환 기능을 구현하지 않는다.
- 기존 `test_workspace/elevator_mission/` 폴더는 수동 엘리베이터 조작 legacy로 남긴다.
- 자동 맵 전환은 새 테스트용 폴더에서 별도 진행한다.
- 현재 상황 분석, 시행 계획, 앞으로의 코드 구현 계획을 상세히 작성한다.
- 2026-06-01과 2026-06-02 일지를 남긴다.

## 수행 내용

- `TODO.md`에 자동 맵 전환 PoC를 후속 작업으로 추가했다.
- 새 폴더 `test_workspace/elevator_auto_map_switch/`를 생성했다.
- 현재 분석 문서 `docs/01_current_analysis.md`를 작성했다.
- 시행 계획 문서 `docs/02_execution_plan.md`를 작성했다.
- 구현 계획 문서 `docs/03_implementation_plan.md`를 작성했다.
- 검증 계획 문서 `docs/04_verification_plan.md`를 작성했다.
- 2026-06-01 일지와 2026-06-02 일지를 작성했다.

## 분석 결론

자동 맵 전환은 기존 mission sequence를 갈아엎을 필요가 없다. 핵심은 기존 수동 `SwitchFloor`와 `floor_orchestrator`의 외부 계약을 유지하면서, 내부 동작을 다음 순서로 자동화하는 것이다.

```text
/elevator/state target floor arrived
  -> Nav2 map server load_map
  -> costmap clear
  -> /initialpose publish
  -> /floor_orchestrator/status ready
```

## 오늘 남긴 경계

- legacy 수동 PoC는 수정하지 않았다.
- production `src/` 패키지는 수정하지 않았다.
- 자동 맵 전환 코드는 아직 구현하지 않았다.
- 새 폴더에는 README와 계획 문서만 생성했다.

## 다음 세션 할 일

1. `test_workspace/elevator_auto_map_switch/docs/03_implementation_plan.md`를 검토한다.
2. `floor_maps.yaml`과 `floor_map_registry.py`부터 TDD로 작성한다.
3. dry-run 상태머신을 만든다.
4. ROS2 package `auto_floor_orchestrator_pkg`를 만든다.
5. 컨테이너에서 Nav2 map server 서비스명을 확인한다.
6. 실제 `load_map` 호출은 dry-run smoke가 통과한 뒤 붙인다.
